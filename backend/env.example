# API Keys
GEMINI_API_KEY=sua_chave_aqui

# Database (opcional - se não definir, usa SQLite local)
# Para produção (Render), adicionar no painel de Environment Variables
DATABASE_URL=postgresql://bo_user:SENHA@host/bo_logs
